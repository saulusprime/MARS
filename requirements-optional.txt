# MARS Beacon — funzionalità opzionali.
# Ognuna migliora un'area; se manca, il modulo corrispondente ripiega su
# un'alternativa e lo dichiara nel referto.

# Embedding reali al posto del proxy char-TFIDF (mars_core.py).
# Trascina torch: sono alcuni GB.
sentence-transformers>=2.2
scikit-learn>=1.3
numpy>=1.24

# Monitoraggio delle citazioni IA e giudizio LLM (mars_citations.py).
anthropic>=0.120

# Scan WAPT con ZAP (mars_wapt.py): NESSUN pacchetto pip serve.
# MARS parla direttamente l'API JSON di ZAP con requests. Il client
# ufficiale python-owasp-zap-v2.4 (0.0.14) NON è utilizzabile: cabla
# l'indirizzo "http://zap/" e ZAP 2.17 non serve più quell'alias
# attraverso il proxy. Serve solo il daemon ZAP in esecuzione.

# Audit WCAG reale via axe-core su Chromium (integrato da C8).
# Serve anche il browser: python -m playwright install chromium,
# e axe-core da npm (npm install axe-core).
playwright>=1.40
